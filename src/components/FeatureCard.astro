---
interface Props {
	title: string;
	description: string;
}

const { title, description } = Astro.props;
---

<article class="card-base p-6">
	<h3 class="text-lg font-semibold text-steel-900">{title}</h3>
	<p class="mt-3 text-sm leading-6 text-steel-600">{description}</p>
</article>
